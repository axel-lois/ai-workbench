# Copy to .env.local and fill in. .env.local is NEVER committed.
# Every ${VAR} used in shared/mcp.json must exist here.

# https://github.com/settings/tokens  (scopes: repo, read:org)
GITHUB_PERSONAL_ACCESS_TOKEN=

# Not variables — binaries that must be on PATH:
#   node 18+ (provides npx)
#   codebase-memory-mcp
